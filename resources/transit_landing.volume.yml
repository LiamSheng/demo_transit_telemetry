resources:
  volumes:
    transit_landing:
      catalog_name: ${var.catalog}
      schema_name: ${var.schema}
      name: transit_landing
      volume_type: MANAGED
      comment: Shared landing volume for raw BC Transit source files.
